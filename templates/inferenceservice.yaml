{{- range $index, $item := .Values.inferenceServices }}
{{- if or (not (hasKey $item "enabled")) $item.enabled }}
{{- if gt $index 0 }}---{{ end }}
{{ include "nuc-kserve.renderResource" (dict "root" $ "item" $item "resourceKey" (printf "inferenceServices[%d]" $index) "defaultApiVersion" $.Values.apiVersions.inferenceService "kind" "InferenceService" "namespaced" true) }}
{{- end }}
{{- end }}
